import React from "react"
import Layout from "../components/Layout"

interface BlogProps {}

const Blog: React.FC<BlogProps> = () => {
  return <Layout>I'm on the Blog page</Layout>
}

export default Blog
