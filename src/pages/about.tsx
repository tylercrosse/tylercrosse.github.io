import React from "react"
import Layout from "../components/Layout"

interface AboutProps {}

const About: React.FC<AboutProps> = () => {
  return <Layout>I'm on the About page</Layout>
}

export default About
