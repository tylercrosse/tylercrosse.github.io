import React from "react"
import Layout from "../components/Layout"

interface IndexProps {}

const Index: React.FC<IndexProps> = () => {
  return <Layout>I'm on the index page</Layout>
}

export default Index
