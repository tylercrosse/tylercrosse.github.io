import React from "react"
import Layout from "../components/Layout"

interface ResumeProps {}

const Resume: React.FC<ResumeProps> = () => {
  return <Layout>I'm on the Resume page</Layout>
}

export default Resume
